import {Menu,X} from 'lucide-react';import {useEffect,useState} from 'react';
const links=[['О ведущем','#about'],['Форматы','#formats'],['Как работаем','#process'],['Отзывы','#reviews'],['Контакты','#contact']];
export function Header(){const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(scrollY>40);addEventListener('scroll',onScroll,{passive:true});return()=>removeEventListener('scroll',onScroll)},[]);return <header className={`site-header ${scrolled?'is-scrolled':''}`}><a href="#top" className="wordmark">Александр <i>Ветров</i></a><nav>{links.map(([label,href])=><a key={href} href={href}>{label}</a>)}</nav><a className="header-cta" href="#contact">Обсудить дату</a><button className="menu-button" aria-label={open?'Закрыть меню':'Открыть меню'} onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button><div className={`mobile-menu ${open?'open':''}`}>{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}</a>)}<a href="#contact" onClick={()=>setOpen(false)}>Обсудить дату</a></div></header>}
