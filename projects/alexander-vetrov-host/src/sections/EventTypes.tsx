import {ArrowUpRight} from 'lucide-react';import {Reveal} from '../components/Reveal';import {eventTypes} from '../data';
export function EventTypes(){return <section className="events section" id="formats"><div className="section-head"><div><span className="section-tag">02 / Форматы</span><h2>У каждого события<br/><em>свой характер</em></h2></div><p>Не переношу один сценарий с праздника на праздник. Формат строится вокруг людей, повода и атмосферы.</p></div><div className="event-grid">{eventTypes.map(([title,text],i)=><Reveal key={title} delay={i*.05} className={`event-card event-${i+1}`}><span>0{i+1}</span><h3>{title}</h3><p>{text}</p><ArrowUpRight/></Reveal>)}</div></section>}
